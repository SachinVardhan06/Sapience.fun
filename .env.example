VITE_APP_NAME=sapience.fun

# GraphQL (Apollo in graphql-server.js). Dev default: http://localhost:4000/
# If port 4000 is busy, server may pick 4001+ — set matching URL here:
# VITE_GQL_URL=http://localhost:4001/
# GQL_PORT=4000
#
# Production: point the SPA at your API subdomain (HTTPS). Set this where you build Vite (Netlify/Vercel/CI env):
# VITE_GQL_URL=https://api.sapience.fun/graphql

# Kalshi proxy (kalshi-proxy.cjs) — optional; use literal \n for PEM line breaks in .env.local
# KALSHI_KEY_ID=
# KALSHI_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
